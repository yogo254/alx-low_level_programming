#include <string.h>
/**
 * _strchr - get initial bytes from s consisting of accept
 * @s: source string
 * @accept: initial bytes to accept

 * Description:  get initial bytes from s consisting of accept
 * Return: int initial bytes lenght
 */
unsigned int _strspn(char *s, char *accept)
{
	int len = strlen(s);

	return (len);
}
