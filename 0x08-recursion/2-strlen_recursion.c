#include <stdio.h>
#include <string.h>
/**
 * _strlen_recursion get String lenght
 * @s: String
 * Description:  print string using reccursion
 * Return: void
 */
int _strlen_recursion(char *s)
{

	int len = strlen(s);

	return (len);
}
